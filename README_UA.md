# WakeLink v1.0
### Розумний Wake-on-LAN • Шифровані команди • Хмарний та локальний режими

**Автор:** [deadboizxc](https://github.com/deadboizxc)  
**Версія:** 1.0 • **Реліз:** Листопад 2025  
**Ліцензія:** NGC License (Neon Genesis Coders) - Безкоштовно для особистого використання

---

## 🚀 Що нового в v1.0

- **Розумний CLI клієнт** - Інтуїтивний інтерфейс командного рядка
- **Покращене шифрування** - ChaCha20 + SHA256 для повної безпеки
- **Уніфікований протокол** - Єдиний протокол для локальних та хмарних пристроїв
- **Керування пристроями** - Збереження конфігурації пристроїв
- **Розширені команди** - OTA, перезавантаження, веб-сервер, крипто-інформація
- **Покращений хмарний реле** - FastAPI сервер з Web UI

---

## 🏗️ Архітектура прошивки

```
firmware/WakeLink/
│
├── WakeLink.ino         # Головний файл (точка входу)
│
├── config.h/cpp         # Конфігурація та EEPROM
├── CryptoManager.h/cpp  # Криптографія (SHA256 + ChaCha20)
├── packet.h/cpp         # Форматування та обробка пакетів
├── command.h/cpp        # Система команд та виконавці
├── tcp_handler.h/cpp    # TCP сервер для локальних команд
├── cloud_client.h/cpp   # Хмарний клієнт для віддаленого керування
├── udp_handler.h/cpp    # UDP для Wake-on-LAN пакетів
├── wifi_manager.h/cpp   # Керування WiFi з'єднаннями
├── web_server.h/cpp     # Веб-інтерфейс для налаштувань
├── ota_manager.h/cpp    # Бездротові оновлення прошивки
└── utils.h/cpp          # Допоміжні утиліти
```

---

## 🔧 Модулі прошивки

### 📋 Core Modules

| Модуль | Призначення |
|--------|-------------|
| **WakeLink.ino** | Головний цикл, ініціалізація, керування станами |
| **config.h/cpp** | Зберігання налаштувань в EEPROM, конфігурація пристрою |
| **CryptoManager** | SHA256 хешування, ChaCha20 шифрування, безпека запитів |
| **packet.h/cpp** | Форматування пакетів, шифрування/дешифрування JSON |

### 🌐 Мережеві модулі

| Модуль | Призначення |
|--------|-------------|
| **tcp_handler** | TCP сервер на порту 99 для локальних команд |
| **cloud_client** | HTTP клієнт для зв'язку з хмарним сервером |
| **udp_handler** | Відправка Wake-on-LAN пакетів по UDP |
| **wifi_manager** | Підключення до WiFi, точка доступу для налаштувань |

### 🖥️ Інтерфейси

| Модуль | Призначення |
|--------|-------------|
| **web_server** | Веб-інтерфейс на порту 80 для конфігурації |
| **command.h/cpp** | Виконавці команд (ping, wake, info, restart, тощо) |
| **ota_manager** | OTA оновлення прошивки через мережу |

---

## ⚡ Швидкий старт

### 1. Встановлення Python клієнта

```bash
# Клонування репозиторію
git clone <repository-url>
cd WakeLink/client

# Встановлення залежностей
pip install requests

# Зробити клієнт виконуваним
chmod +x wl

# Додати в PATH (опціонально)
sudo ln -s $(pwd)/wl /usr/local/bin/wl
```

### 2. Компіляція прошивки в Arduino IDE

1. **Відкрийте** `WakeLink.ino` в Arduino IDE
2. **Встановіть необхідні бібліотеки:**
   - ArduinoJson
   - ESP8266WiFi (для ESP8266) або WiFi (для ESP32)
3. **Оберіть плату:**
   - Для ESP8266: NodeMCU 1.0 (ESP-12E Module)
   - Для ESP32: ESP32 Dev Module
4. **Налаштуйте порт** та завантажте прошивку

### 3. Початкова налаштування пристрою

1. **Підключіться** до точки доступу `WakeLink-Setup` (пароль: `configure123`)
2. **Відкрийте** `http://192.168.4.1` у браузері
3. **Налаштуйте:**
   - WiFi мережу (SSID та пароль)
   - Device Token (автоматично генерується)
   - Хмарний сервер (опціонально)

### 4. Використання з CLI клієнтом

```bash
# Додати локальний пристрій
wl --add-device myesp --ip 192.168.1.100 --token YOUR_DEVICE_TOKEN

# Основні команди
wl d myesp info                    # Інформація про пристрій
wl device myesp ping               # Перевірка зв'язку
wl -d myesp wake AA:BB:CC:DD:EE:FF # Wake-on-LAN
wl d myesp restart                 # Перезавантаження
wl device myesp ota                # OTA оновлення

# Додати хмарний пристрій (якщо налаштовано)
wl --add-device mycloud --token DEVICE_TOKEN --api-token API_TOKEN --cloud
```

---

## 🔐 Система безпеки

### Криптографія
- **SHA256** - для хешування токенів та ключів
- **ChaCha20** - потокове шифрування для команд
- **Nonce** - унікальні значення для кожного пакету

### Формат безпечного пакету
```
[2 байти: довжина даних] + [шифротекст] + [16 байт: nonce]
```

### Аутентифікація
- **Device Token** - унікальний ідентифікатор пристрою
- **API Token** - для хмарного доступу (опціонально)

---

## 📡 Протокол зв'язку

### Локальний режим (TCP)
```cpp
// Порт: 99
// Протокол: зашифровані JSON команди
{
  "command": "wake",
  "data": {"mac": "AA:BB:CC:DD:EE:FF"},
  "device_id": "esp01",
  "timestamp": 1630000000000
}
```

### Хмарний режим (HTTP)
```cpp
// Ендпоінти: /api/push, /api/pull
// Транспорт: зашифровані команди через HTTP
```

---

## 🛠️ Доступні команди

### Основні команди
- `ping` - перевірка доступності
- `info` - інформація про пристрій
- `wake` - відправка WOL пакету
- `restart` - перезавантаження

### Адміністративні
- `ota_start` - запуск OTA оновлення
- `open_setup` - відкриття точки доступу
- `web_control` - керування веб-сервером

### Безпека
- `crypto_info` - інформація про криптосистему
- `counter_info` - статистика запитів
- `reset_counter` - скидання лічильника

---

## 🔧 Розширені налаштування

### Конфігурація через EEPROM
```cpp
struct DeviceConfig {
    char device_token[128];
    char wifi_ssid[32];
    char wifi_pass[64];
    char device_id[24];
    char server_url[128];
    uint8_t wifi_configured;
    uint8_t cloud_enabled;
    uint8_t web_server_enabled;
};
```

### Налаштування пінодів
```cpp
#define STATUS_LED 2        // LED індикатор
#define RESET_BUTTON_PIN 0  // Кнопка скидання
#define TCP_PORT 99         // Порт TCP сервера
#define UDP_PORT 9          // Порт для WOL
```

---

## 🐛 Вирішення проблем

### Поширені проблеми:

1. **Пристрій не підключається до WiFi**
   - Перевірте SSID та пароль
   - Переконайтесь у силі сигналу

2. **TCP з'єднання відхилено**  
   - Перевірте що TCP сервер запущений (порт 99)
   - Переконайтесь у правильності IP адреси

3. **Помилки шифрування**
   - Перевірте збіг device_token на клієнті та пристрої
   - Спробуйте перезавантажити пристрій

4. **WOL пакети не відправляються**
   - Перевірте MAC адресу та мережеві налаштування
   - Переконайтесь що пристрій в одній мережі з ціллю

5. **Проблеми з компіляцією в Arduino IDE**
   - Переконайтесь що всі файли в одній папці
   - Перевірте версії бібліотек

---

## 🔄 OTA Оновлення

### Автоматичне оновлення
```bash
# Ініціювати OTA оновлення
wl d myesp ota

# Пристрій буде доступний для оновлення 30 секунд
# Використовуйте Arduino IDE для завантаження нової прошивки
```

### Ручне оновлення через Arduino IDE
1. Викличте `wl d myesp ota`
2. В Arduino IDE оберіть порт OTA 
3. Завантажте нову версію прошивки
4. Пристрій автоматично перезавантажиться

---

## 📊 Моніторинг та діагностика

### Serial Monitor
```bash
# Arduino IDE
Tools → Serial Monitor (115200 baud)
```

### Статистика роботи
- Кількість запитів
- Стан WiFi з'єднання
- Вільна пам'ять
- Стан криптосистеми

### Діагностичні команди
```bash
wl d myesp info        # Детальна інформація
wl d myesp crypto      # Стан криптосистеми
```

---

## ☁️ Розгортання хмарного сервера

### Docker Compose
```yaml
services:
  wakelink-server:
    build: ./server
    ports: ["9009:9009"]
    restart: unless-stopped
```

```bash
docker-compose up -d
```

### Ручне встановлення
```bash
cd server
pip install -r requirements.txt
python main.py
```

---

## 🤝 Розробка та внески

### Структура прошивки для Arduino IDE
- Усі файли повинні знаходитись в одній папці
- Головний файл має називатись як папка (.ino)
- Додаткові файли (.h/.cpp) автоматично включаються

### Тестування
1. Використовуйте Serial Monitor для діагностики
2. Тестуйте команди через CLI клієнт
3. Перевіряйте роботу в обох режимах (локальний/хмарний)

---

## 📄 Ліцензія

**NGC License (Neon Genesis Coders)**  
Copyright (c) 2025-present deadboizxc

- ✅ Дозволено: особисте використання, модифікації, self-hosting
- ❌ Заборонено: комерційне використання, SaaS, перерозподіл
- 🔒 Потрібен дозвіл автора для корпоративного використання

---

**WakeLink Firmware v1.0**  
*Безпечне віддалене керування для ESP8266/ESP32*